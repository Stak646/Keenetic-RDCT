# Release v{VERSION}

## Changes
- 

## Compatibility
- Schema version: {SCHEMA_VERSION}
- Minimum compatible: {SCHEMA_VERSION - 1}
- Config version: {CONFIG_VERSION}

## Artifacts
- `keenetic-debug-{VERSION}-offline-mipsel.tar.gz` (SHA256: ...)
- `keenetic-debug-{VERSION}-offline-aarch64.tar.gz` (SHA256: ...)
- `release-manifest.json`

## Install/Upgrade
```shell
curl -fsSL .../install.sh | sh
# or
sh install.sh --upgrade
```

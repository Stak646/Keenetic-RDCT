# Configuration Guide
See `schemas/config.schema.json` for full field reference. Key config file: `config.json`.
Priority: CLI flags > env vars > config.json > built-in defaults.
See `examples/config.full.json` for all fields with descriptions.
